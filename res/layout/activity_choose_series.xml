<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/layout_choose_series"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="5dp"
    tools:context=".shows.ChooseSeries" >

    <Spinner
        android:id="@+id/spins"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentTop="true" />

    <TextView
        android:id="@+id/spins_overview"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_above="@+id/spins_choose"
        android:layout_alignParentLeft="true"
        android:layout_below="@id/spins"
        android:scrollbars="vertical"
        android:visibility="gone" />

    <Button
        android:id="@id/spins_choose"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:onClick="add"
        android:visibility="visible" />

    <Button
        android:id="@+id/spins_cancel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentLeft="true"
        android:onClick="cancel"
        android:text="@string/cancel"
        android:visibility="visible" />

</RelativeLayout>